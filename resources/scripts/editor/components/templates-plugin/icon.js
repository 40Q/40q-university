/**
 * WordPress dependencies
 */

const icon = (
  <p className="template-selector font-sans rounded-sm px-3 py-2 bg-primary-dark text-back-light">
    Select Template
  </p>
);

export default icon;
